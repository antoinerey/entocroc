import styled from 'utils/styled'
import { fonts } from 'theme'

const H1 = styled('h1', {
  margin: 0,
  lineHeight: 1.3,
  fontSize: '5rem',
  fontFamily: fonts.bazar,
  textTransform: 'uppercase',
})

export default H1
