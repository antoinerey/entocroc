import styled from 'utils/styled'
import { fonts } from 'theme'

const H2 = styled('h2', {
  margin: 0,
  lineHeight: 1.3,
  fontSize: '2rem',
  fontFamily: fonts.bazar,
  textTransform: 'uppercase',
})

export default H2
