import styled from 'utils/styled'
import { fonts } from 'theme'

const H3 = styled('h3', {
  margin: 0,
  lineHeight: 1.3,
  fontSize: '1.25rem',
  fontFamily: fonts.bazar,
  textTransform: 'uppercase',
})

export default H3
