import styled from 'utils/styled'
import { colors } from 'theme'

const Small = styled('small', {
  display: 'block',
  fontSize: '0.875rem',
  lineHeight: 1.35,
  color: colors.grey,
})

export default Small
